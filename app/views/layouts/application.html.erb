<!DOCTYPE html>
<html>
  <head>
    <title>Demo</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header>
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
          <a class="navbar-brand d-flex w-50 me-auto" href="/">LOGO</a>
          <% if current_user %>
            <div>
              Welcome: <%= current_user.email %>
              <%= link_to "Sign out", destroy_user_session_path(authenticity_token: form_authenticity_token), data: { turbo: true, turbo_method: :delete, turbo_confirm: 'Are you sure?' } %>
            </div>
          <% else %>
            <div><%= link_to "Login", new_user_session_path %></div>
          <% end%>
        </div>
      </nav>
    </header>
    <div class="container-fluid">
      <div class="row">
        <div class="col-md-3">
          <div class="list-group">
            <%= link_to "Publisher List", publishers_path, class: "list-group-item" %>
          </div>
        </div>
        <div class="col-md-9">
          <%= yield %>
        </div>
      </div>
    </div>
  </body>
</html>
